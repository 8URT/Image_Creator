server {
    listen 80;
    server_name tools.lemauricien.com;

    # Redirect to HTTPS (uncomment after SSL setup)
    # return 301 https://$server_name$request_uri;

    # For now, serve HTTP
    location / {
        proxy_pass http://127.0.0.1:8000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    # Static files
    location /static {
        alias /path/to/Social_Image_Creator/app/static;
        expires 30d;
        add_header Cache-Control "public, immutable";
    }
}

# HTTPS configuration (uncomment after SSL certificate setup)
# server {
#     listen 443 ssl http2;
#     server_name tools.lemauricien.com;
#
#     ssl_certificate /etc/letsencrypt/live/tools.lemauricien.com/fullchain.pem;
#     ssl_certificate_key /etc/letsencrypt/live/tools.lemauricien.com/privkey.pem;
#
#     location / {
#         proxy_pass http://127.0.0.1:8000;
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto $scheme;
#     }
#
#     location /static {
#         alias /path/to/Social_Image_Creator/app/static;
#         expires 30d;
#         add_header Cache-Control "public, immutable";
#     }
# }
